import { CustomMaterialModule } from './custom-material-module';

describe('CustomMaterialModule', () => {
  it('should create an instance', () => {
    expect(new CustomMaterialModule()).toBeTruthy();
  });
});
